# Firefox Add-ons listing assets

Reserved for a later AMO screenshot and promo pipeline.

Do not put Chrome templates, captures, or generated PNGs here. Chrome
listing sources live in `../chrome/`; composited Chrome bitmaps go to
`../generated/chrome/`.
