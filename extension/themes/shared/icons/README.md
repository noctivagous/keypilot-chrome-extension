# Theme icon packs

Semantic names (`close`, `collapse`, `gear`, `keyboard`, …) resolve through
`getThemeIconUrl`. Put overrides only in `themes/<theme-id>/icons/` and list
them in that theme's `icons.overrides`. Missing files fall back to `shared/`.
